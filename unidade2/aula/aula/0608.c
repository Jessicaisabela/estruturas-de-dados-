#include <stdio.h>

int main(){
FILE*fp;
fp = fopen("entrada.txt", "rt");
if(fp==NULL){
    printf("erro na abertura do arquivo!\n");
    exit(1);
}


/*função para abrir arquivo - fopen("arquivo.o", "br");
para fechar fclose(t);
*/
}